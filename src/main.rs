use hangman::start_game;

fn main() {
    start_game();
    // println!("Hello, world!");
    // initiate game loop
    // display hangman, guessed letters, word progress
    // take letter
    // if already guessed, ask to try again
    // if not, place if correct, add limb if not
    // when all limbs are present, game over
}
